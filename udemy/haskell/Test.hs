module Test where

division :: Int -> Int -> Int   -- Declaración del tipo de la función
division x y = x `div` y    -- Declaración del cuerpo de la función

cuadrado :: Int -> Int
cuadrado x = x * x

suma :: Int -> Int -> Int
suma x y = x + y